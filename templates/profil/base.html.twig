{% extends 'base.html.twig' %}

{% block title %}Profile
{% endblock %}

{% block body %}
	<div class="position-absolute top-0 d-flex flex-row" style="height:100vh; width:100vw">
		<div class="bg-secondary-subtle h-100" style="width:20%">
			<br class="mt-5">
			<div class="mt-5 container text-center">
				<h2>{{ app.user.nom }}
					{{ app.user.prenom }}</h2>
				<hr>
				<div class="d-grid gap-2 col-8 mx-auto">
					<a class="btn btn-light " href="{{ path("app_addPresta") }}">Ajouter une prestation</absolute>
					<a class="btn btn-light" href="{{ path("app_profilPrestaList") }}">Liste des prestations</a>
				</div>

			</div>

		</div>
		<div class="h-100" style="width:80%">
			{% block content %}
				<div class="h-100 ">
					<div style="width:80%" class="text-center position-absolute top-50">
						<h1 class="">Bienvenue sur votre Profile</h1>
					</div>
				</div>
			{% endblock %}
		</div>
	</div>
{% endblock %}
